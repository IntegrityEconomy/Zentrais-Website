# Exchange Backend Environment Variables

# Database
POSTGRES_URI=postgresql://postgres:postgres@localhost:5432/exchange_db

# JWT Secret - MUST match Dialogue backend for unified auth
JWT_SECRET=change-me

# Server
PORT=3002
NODE_ENV=development

# Enable/Disable database connection
ENABLE_DB=true
